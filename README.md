# website-middleman

Make sure you are using Ruby 2.7.8

To run project:
```aiignore
bundle install
bundle exec middleman server
```

To build files for production:
```aiignore
bundle exec middleman build
```